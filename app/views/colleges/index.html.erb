<!DOCTYPE html>
<html>
<head>
<style>
table, th, td {
    border: 1px solid black;
    border-collapse: collapse;
}
th, td {
    padding: 5px;
}
th {
    text-align: left;
    height: 50px;
}
</style>
</head>

<body>
<form action = "/colleges/results" method = "get" style = "float:right" placeholder = "search" >
  <input type="search" name="Search" />
</form><br>
<table style="width:100%">
	<caption><h2>List of Colleges</h2></caption>
  <tr>
    <th>Id</th>
  	<th>Name</th>
    <th>Established Year</th>
    
    <th colspan="3">Operations</th>
  </tr>

  <% @colleges.each do |s| %>   
    <tr>
	    <td> <%= link_to s[:id], college_path(s) %></td>
	    <td> <%= s[:name] %></td>
	    <td> <%= s[:year] %></td>
      
      <td> <%= link_to image_tag("Edit_Icon_32.png"), edit_college_path(s), method: :get  %></td>
      <td><%= button_to "Delete College", college_path(s),method: :delete, data: { confirm: "Are you sure?" } %></td>
      <td> <%= link_to image_tag("psearch.png"), search_college_path(s) %></td>
    </tr>
  <% end %>

</table>
<p><%= link_to "Create new College", { action: "new" } %></p>

</body>
</html>
